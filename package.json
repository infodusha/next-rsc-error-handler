{
  "name": "root",
  "version": "0.0.0",
  "scripts": {
    "dev": "cd examples/general && next dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "infodusha",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
